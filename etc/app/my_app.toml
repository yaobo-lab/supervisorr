name = "my_app"
autostart = true
autorestart = true
command = "echo 'Replace this with your process command!'"
directory = "/home/luke/supervisord"
stdout_logfile = "/home/luke/supervisord/my_app.log"
stderr_logfile = "/home/luke/supervisord/my_app.err"

[environment]
PORT = "8080"
NODE_ENV = "production"